---
const { id } = Astro.props;
const { classname } = Astro.props;
---

<section id={id} class={"px-5 flex justify-center overflow-hidden lg:block " + classname}>
  <div class="w-full max-w-[1100px] mx-auto">
    <slot />
  </div>
</section>
